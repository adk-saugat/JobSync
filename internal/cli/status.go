package cli

import "fmt"

func runStatus(args []string) error {
	_ = args
	// Phase 0 stub — later: last sync, next run time, quota / pending mail.
	fmt.Println("status: not implemented yet (Phase 0 stub)")
	return nil
}
